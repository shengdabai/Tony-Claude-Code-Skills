node_modules/
**/node_modules/
.DS_Store
*.bak

# 运行时日志 / Python 编译缓存 / 备份(机器专属产物,不进公开仓)
**/*.log
**/*.err
**/*.out
**/__pycache__/
**/*.pyc
**/*.pyo
*.claude-bak-*
**/*.claude-bak-*

# 会话元数据(含本机目录结构,不公开)
**/sessions_extracted.json

# Gumroad 商业运营数据(上架进度/商品清单)
**/gumroad-data/
**/gumroad-data-core/

# 内部开发 ledger / 运行时状态(非 skill 源)
my-config/scripts/gpt5pro/todo.md
skills/xiaolai/claude-agent-sdk/agent/state.json

# Build / compile artifacts (skill code is source-only, runtime built elsewhere)
**/dist/
**/build/
**/.next/
**/out/
*.tsbuildinfo
.omc/
local-config/
claude-home/
settings.json
settings.local.json
.env
.env.*
*.key
*.pem
config/settings.json

# 浏览器自动化运行时数据 (含加密 key/policy_token), 永远不进仓
**/browser_state/
**/browser_profile/

# 第三方源仓库克隆缓存(避免嵌套 .git 破坏 push)
skills/_repos/

# RuVector 本地 DB(含会话数据)
ruvector.db
**/ruvector.db

# 孤儿污染目录(skills/0 是 stitch-design-taste 的错位副本)
skills/0/

# macOS 冲突副本(iCloud / Finder 同步残留)
**/* 2.*

# .gitignore 自身的冲突副本
**/.gitignore 2

# LICENSE 冲突副本
**/LICENSE 2

# 密钥/凭证
*.pem
*.key
**/credentials.*
**/secrets.*
.aws/
.ssh/

# Claude 个人化文件(不应公开)
my-config/CLAUDE.local.md
my-config/projects/-Users-tonysheng/memory/user_*.md
my-config/projects/-Users-tonysheng/memory/feedback_no-pii-storage.md

# AI 历史归档(含个人对话)
my-config/AI-Archive/
**/.claude.json
**/sessions/

# 任何 .env / .envrc
**/.env
**/.env.*
**/.envrc
# Codex governance: local secrets and machine state
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.p12
*.pfx
id_rsa
id_dsa
.npmrc
.DS_Store
.omc/
.codex/
.claude/local/
